{
  "name": "vibemeet-backend",
  "version": "1.0.0",
  "description": "VibeMeet self-hostable Node/Express + Socket.IO server (reference).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start":  "node -v && npm list mongodb && node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "mongodb": "^7.6.0",
    "socket.io": "^4.7.5"
  }
}
